import Component from "$store/components/header/Alert.tsx";
import type { Props } from "$store/components/header/Alert.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
