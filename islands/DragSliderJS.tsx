import Component from "$store/components/ui/DragSliderJS.tsx";
import type { Props } from "$store/components/ui/DragSliderJS.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
