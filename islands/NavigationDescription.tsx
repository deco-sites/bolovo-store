import Component from "$store/components/ui/NavigationDescription.tsx";
import type { Props } from "$store/components/ui/NavigationDescription.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
