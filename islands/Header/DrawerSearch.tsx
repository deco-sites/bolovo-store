import Component from "$store/components/header/DrawerSearch.tsx";
import type { Props } from "$store/components/header/DrawerSearch.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
