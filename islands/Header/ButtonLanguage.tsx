import Component from "$store/components/header/Buttons/Language.tsx";
import type { Props } from "$store/components/header/Buttons/Language.tsx";

export default function Island(props: Props) {
  return <Component {...props} />;
}
